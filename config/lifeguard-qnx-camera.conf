# Raspberry Pi 5 / QNX Camera Module 3 sender. No TensorFlow is used here.
camera_backend = csi
camera_unit    = 1
frame_width    = 2304
frame_height   = 1296
target_fps     = 30

# Unused by camera_test, retained so one Config format serves both programs.
display        = false
stream_enabled = false
